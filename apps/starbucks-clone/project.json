{
  "name": "starbucks-clone",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/starbucks-clone/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/starbucks-clone"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "starbucks-clone:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "starbucks-clone:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "starbucks-clone:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "starbucks-clone:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "starbucks-clone:build:development"
        },
        "production": {
          "buildTarget": "starbucks-clone:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/starbucks-clone"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/starbucks-clone/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
